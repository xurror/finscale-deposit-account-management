apply plugin: "war"

bootWar {
    mainClassName = "org.muellners.finscale.deposit.DepositAccountManagementApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
